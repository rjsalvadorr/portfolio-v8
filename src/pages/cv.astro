---
import Layout from "../layouts/BlogPost.astro";
import { getCollection, render } from "astro:content";

const cvData = (await getCollection("pages")).find(data => data.id == 'cv')
console.log(cvData)
const { Content } = await render(cvData);
---

<Layout {...cvData.data}>
	<Content />
</Layout>
